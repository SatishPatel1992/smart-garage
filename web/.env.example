# Optional: Backend API URL. If not set, the app uses the Vite dev proxy (/api -> http://localhost:3000).
# For production build, set this to your API URL, e.g. https://api.yourgarage.com
# VITE_API_URL=http://localhost:3000
